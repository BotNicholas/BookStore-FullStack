import { NotMoreThanDirective } from './not-more-than.directive';

describe('NotMoreThanDirective', () => {
  it('should create an instance', () => {
    const directive = new NotMoreThanDirective();
    expect(directive).toBeTruthy();
  });
});

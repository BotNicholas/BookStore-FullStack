import { AdultDirective } from './adult.directive';

describe('AdultDirective', () => {
  it('should create an instance', () => {
    const directive = new AdultDirective();
    expect(directive).toBeTruthy();
  });
});
